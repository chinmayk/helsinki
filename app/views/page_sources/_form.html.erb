<%= form_for(@page_source) do |f| %>
  <% if @page_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_source.errors.count, "error") %> prohibited this page_source from being saved:</h2>

      <ul>
      <% @page_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :links_path %><br />
    <%= f.text_field :links_path %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
